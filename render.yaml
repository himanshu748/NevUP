services:
  - type: web
    name: trademind-ai-engine
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: trademind-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: trademind-redis
          type: redis
          property: connectionString
      - key: APP_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_ALGORITHM
        value: HS256
      - key: JWT_EXPIRE_HOURS
        value: "24"
      - key: HF_TOKEN
        sync: false
      - key: LOG_LEVEL
        value: INFO

  - type: redis
    name: trademind-redis
    plan: free
    maxmemoryPolicy: noeviction

databases:
  - name: trademind-postgres
    plan: free
